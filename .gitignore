node_modules
dist
.env
.env.*
!.env.example
*.pem
*.key
tsconfig.tsbuildinfo
*.db
*.db-shm
*.db-wal
.worktrees
.agent_logs
local.db

# Claude agent worktrees & internal state (transient — never commit)
.claude/

# Secrets & backend config (never commit)
db/.secret.key
db/backend.json

# Cloned user projects (never commit)
projects/

# Playwright artifacts
test-results/
playwright-report/
